{
  "name": "merchanex",
  "version": "1.0.0",
  "description": "The go to handyman app.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ./client && npm run build --prod && cp -r build ../"
  },
  "dependencies": {
    "expo": "^51.0.16"
  }
}
